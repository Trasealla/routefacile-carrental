export enum MemoDocumentStatus {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  ARCHIVED = 'archived',
}

export enum MemoAccessTargetType {
  ALL = 'all',
  USER_TYPE = 'user_type',
  USER = 'user',
}
